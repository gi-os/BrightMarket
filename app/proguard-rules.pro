# R8 is off for release in v1 (isMinifyEnabled = false), so this is a placeholder
# kept so turning it on later is a one-line change rather than a debugging session.
-dontwarn org.jetbrains.annotations.**
